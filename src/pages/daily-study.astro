---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Chok Breslov - Daily Torah Study Schedule" description="Complete the entire Breslov library in one year with daily Torah study portions. Likutay Moharan, Likutay Halachos, Sichos HaRan, Likutay Tefilos, and more. Track your progress day by day." breadcrumbs={[{name: "Home", url: "https://ajew.org"}, {name: "Daily Study", url: "https://ajew.org/daily-study"}]} jsonLd={{
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Chok Breslov - Daily Torah Study",
  "description": "Complete the entire Breslov library in one year with daily Torah study portions.",
  "url": "https://ajew.org/daily-study",
  "isPartOf": { "@type": "WebSite", "name": "A Jew", "url": "https://ajew.org" }
}}>
  <div class="chok-container" id="chokApp">
    <!-- Header -->
    <header class="chok-header">
      <h1 class="chok-hebrew-title">חק ברסלב</h1>
      <h2 class="chok-english-title">Daily Torah Study</h2>
      <p class="chok-subtitle">Complete the entire Breslov library in one year<br><span style="font-family: var(--font-hebrew); font-size: 0.85em; opacity: 0.8;" dir="rtl">השלם את כל ספריית ברסלב בשנה אחת</span></p>
      <div class="chok-date-info" id="dateInfo">
        <span class="hebrew-date" id="hebrewDateDisplay">Loading...</span>
        <span class="day-number" id="dayNumber"></span>
      </div>
      <div class="chok-progress-bar">
        <div class="chok-progress-fill" id="progressFill"></div>
      </div>
      <p class="chok-progress-text" id="progressText"></p>
      <p class="chok-sibling"><a href="/reader/chok">חק לישראל</a> <span>— the classic daily chok: Tanach, Mishna, Gemara, halacha &amp; Zohar</span></p>
    </header>

    <!-- Navigation -->
    <nav class="chok-nav">
      <button class="chok-nav-btn" id="prevDay" title="Previous day">&larr; Yesterday</button>
      <button class="chok-nav-btn chok-nav-today" id="todayBtn" title="Go to today">Today</button>
      <button class="chok-nav-btn" id="nextDay" title="Next day">Tomorrow &rarr;</button>
    </nav>
    <div class="chok-nav-extra">
      <label>
        Jump to day:
        <input type="number" id="dayPicker" min="1" max="354" class="chok-day-input" />
      </label>
      <button class="chok-nav-btn chok-small-btn" id="resetBtn" title="Start from Day 1">Reset to Day 1</button>
    </div>

    <!-- Stats bar -->
    <div class="chok-stats" id="statsBar">
      <div class="chok-stat">
        <span class="stat-num" id="streakCount">0</span>
        <span class="stat-label">Day Streak</span>
      </div>
      <div class="chok-stat">
        <span class="stat-num" id="completedCount">0</span>
        <span class="stat-label">Days Done</span>
      </div>
      <div class="chok-stat">
        <span class="stat-num" id="overallPercent">0%</span>
        <span class="stat-label">Year Progress</span>
      </div>
    </div>

    <!-- Date Letters (Ebay HaNachal) -->
    <section class="chok-date-letters" id="dateLettersSection" style="display:none;">
      <h3 class="section-title">Today's Letter</h3>
      <div id="dateLettersContent"></div>
    </section>

    <!-- Today's Portions -->
    <section class="chok-portions" id="portionsGrid">
      <p class="loading-msg">Loading daily study schedule...</p>
    </section>

    <!-- Bonus Section -->
    <section class="chok-bonus-section" id="bonusSection" style="display:none;">
      <h3 class="section-title bonus-title">Bonus Study</h3>
      <div id="bonusContent"></div>
    </section>
  </div>

<style is:global>
  /* ═════════════════════════════════════════════════════════════
     חק ברסלב — deluxe presentation, sharing the Chok LiYisroel
     sefer token system (same palette/typography as /reader/chok).
     All hooks (ids/classes) preserved; JS-generated markup styled
     globally under .chok-container.
     ═════════════════════════════════════════════════════════════ */
  .chok-container {
    --font-he: 'Noto Serif Hebrew', 'David Libre', 'Frank Ruhl Libre', serif;
    --font-en: 'EB Garamond', Georgia, 'Times New Roman', serif;
    --font-ui: 'Open Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    --ink: #2f2519;
    --paper: #fbf7ee;
    --panel: #f4eddc;
    --panel2: #fffdf6;
    --border: #e3d8bf;
    --border-strong: #cfc09a;
    --gold: #a8842e;
    --gold-bright: #c9a227;
    --gold-soft: #e7d9ae;
    --gold-wash: rgba(168, 132, 46, 0.08);
    --teal: #177f70;
    --teal-deep: #106154;
    --muted: #8d7f66;
    --muted2: #6f6350;
    --ok: #2d6a4f;
    --shadow1: 0 1px 2px rgba(63, 48, 26, 0.06), 0 6px 18px -6px rgba(63, 48, 26, 0.12);
    --shadow2: 0 2px 4px rgba(63, 48, 26, 0.08), 0 14px 34px -10px rgba(63, 48, 26, 0.2);
    max-width: 900px;
    margin: 0 auto;
    padding: 1.6rem 1rem 4rem;
    color: var(--ink);
    font-family: var(--font-ui);
  }
  body.dark-mode .chok-container {
    --ink: #efe6d2;
    --paper: #171310;
    --panel: #221c15;
    --panel2: #2a2219;
    --border: #3c3226;
    --border-strong: #5a4a2f;
    --gold: #c9a227;
    --gold-bright: #e2c04c;
    --gold-soft: #6b5524;
    --gold-wash: rgba(201, 162, 39, 0.1);
    --teal: #2aa392;
    --teal-deep: #1d7c6f;
    --muted: #a3937a;
    --muted2: #857455;
    --shadow1: 0 1px 2px rgba(0, 0, 0, 0.4), 0 6px 18px -6px rgba(0, 0, 0, 0.5);
    --shadow2: 0 2px 4px rgba(0, 0, 0, 0.45), 0 14px 34px -10px rgba(0, 0, 0, 0.6);
  }
  .chok-header { text-align: center; padding: 1.4rem 1rem 1.2rem; background: linear-gradient(180deg, var(--gold-wash), transparent 70%); border: 1px solid var(--border); border-radius: 18px; box-shadow: var(--shadow1); }
  .chok-hebrew-title { font-family: var(--font-he); font-size: clamp(2.2rem, 6vw, 3.2rem); font-weight: 700; color: var(--ink); letter-spacing: 0.02em; margin: 0; }
  .chok-hebrew-title::after { content: ''; display: block; width: 84px; height: 2px; margin: 0.55rem auto 0.3rem; background: linear-gradient(90deg, transparent, var(--gold), transparent); }
  .chok-english-title { font-family: var(--font-en); font-size: 1.35rem; font-weight: 600; color: var(--gold); margin: 0.2rem 0 0.4rem; letter-spacing: 0.04em; }
  .chok-subtitle { color: var(--muted2); font-size: 0.98rem; margin: 0; line-height: 1.6; }
  .chok-date-info { display: flex; justify-content: center; gap: 0.8rem; align-items: baseline; margin: 0.8rem 0 0.6rem; flex-wrap: wrap; }
  .hebrew-date { font-family: var(--font-he); font-size: 1.08rem; color: var(--ink); }
  .day-number { color: var(--muted2); font-size: 0.85rem; letter-spacing: 0.06em; text-transform: uppercase; }
  .chok-progress-bar { height: 10px; border-radius: 999px; background: var(--panel); border: 1px solid var(--border); overflow: hidden; max-width: 560px; margin: 0.5rem auto 0.35rem; }
  .chok-progress-fill { height: 100%; width: 0%; border-radius: 999px; background: linear-gradient(90deg, var(--gold), var(--gold-bright)); transition: width 0.5s ease; }
  .chok-progress-text { color: var(--muted2); font-size: 0.82rem; margin: 0.3rem 0 0; }
  .chok-sibling { margin: 0.9rem 0 0; font-size: 0.88rem; color: var(--muted2); }
  .chok-sibling a { color: var(--teal-deep); font-family: var(--font-he); font-weight: 700; text-decoration: none; border-bottom: 1px solid var(--gold-soft); padding-bottom: 1px; }
  .chok-sibling a:hover { color: var(--gold); }
  .chok-nav { display: flex; justify-content: center; gap: 0.55rem; margin: 1.2rem 0 0.5rem; flex-wrap: wrap; }
  .chok-nav-btn { min-height: 44px; padding: 0.55rem 1.15rem; border-radius: 12px; border: 1px solid var(--border-strong); background: var(--panel2); color: var(--ink); font-family: var(--font-ui); font-size: 0.92rem; cursor: pointer; box-shadow: var(--shadow1); transition: transform 0.12s ease, box-shadow 0.12s ease, border-color 0.12s ease; display: inline-flex; align-items: center; }
  .chok-nav-btn:hover { transform: translateY(-1px); border-color: var(--gold); box-shadow: var(--shadow2); }
  .chok-nav-btn:active { transform: translateY(0); }
  .chok-nav-today { background: linear-gradient(180deg, var(--gold), #8f6f22); border-color: var(--gold); color: #fffdf6; font-weight: 700; }
  body.dark-mode .chok-nav-today { background: linear-gradient(180deg, var(--gold-bright), var(--gold)); color: #171310; }
  .chok-small-btn { min-height: 34px; padding: 0.3rem 0.8rem; font-size: 0.8rem; }
  .chok-nav-extra { display: flex; justify-content: center; align-items: center; gap: 0.9rem; margin: 0 0 1rem; color: var(--muted2); font-size: 0.88rem; flex-wrap: wrap; }
  .chok-day-input { width: 84px; min-height: 38px; text-align: center; border: 1px solid var(--border-strong); border-radius: 10px; background: var(--panel2); color: var(--ink); font-size: 0.95rem; }
  .chok-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.7rem; max-width: 640px; margin: 0 auto 1.4rem; }
  .chok-stat { text-align: center; padding: 0.7rem 0.5rem; background: var(--panel2); border: 1px solid var(--border); border-radius: 14px; box-shadow: var(--shadow1); }
  .stat-num { display: block; font-family: var(--font-en); font-size: 1.45rem; font-weight: 700; color: var(--gold); line-height: 1.15; }
  .stat-label { font-size: 0.72rem; letter-spacing: 0.08em; text-transform: uppercase; color: var(--muted2); }
  .section-title { font-family: var(--font-en); font-size: 1.05rem; font-weight: 600; letter-spacing: 0.05em; color: var(--gold); margin: 1.6rem 0 0.7rem; text-align: center; }
  .section-title::before, .section-title::after { content: ' · '; color: var(--gold-soft); }
  .bonus-title { color: #2d6a4f; }
  body.dark-mode .bonus-title { color: #7ecfa8; }
  .date-letter-card { display: flex; gap: 0.9rem; align-items: center; background: var(--panel2); border: 1px solid var(--border); border-radius: 14px; box-shadow: var(--shadow1); padding: 0.85rem 1rem; margin-bottom: 0.6rem; }
  .letter-info { flex: 1; }
  .letter-sub { color: var(--muted2); font-size: 0.85rem; margin-top: 0.15rem; }
  .date-letter-card a { color: var(--teal-deep); text-decoration: none; border-bottom: 1px solid var(--gold-soft); }
  .chok-portions { display: grid; gap: 0.75rem; }
  .book-card { background: var(--panel2); border: 1px solid var(--border); border-left: 3px solid var(--gold-soft); border-radius: 14px; box-shadow: var(--shadow1); padding: 0.95rem 1.05rem; transition: box-shadow 0.15s ease, border-color 0.15s ease; }
  .book-card:hover { border-color: var(--gold); box-shadow: var(--shadow2); }
  .book-card.completed { border-left-color: #2d6a4f; background: linear-gradient(0deg, rgba(45, 106, 79, 0.05), var(--panel2)); }
  .card-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 0.8rem; }
  .book-name-he { font-family: var(--font-he); font-size: 1.18rem; font-weight: 700; color: var(--ink); margin: 0; }
  .book-name-en { color: var(--muted2); font-size: 0.86rem; margin: 0.1rem 0 0.45rem; }
  .section-ref { color: var(--muted2); font-size: 0.85rem; margin: 0.45rem 0 0.5rem; line-height: 1.55; }
  .book-progress { height: 5px; border-radius: 999px; background: var(--panel); border: 1px solid var(--border); overflow: hidden; margin-top: 0.55rem; }
  .book-progress-fill { height: 100%; width: 0%; background: linear-gradient(90deg, var(--gold), var(--gold-bright)); }
  .check-btn { width: 44px; height: 44px; min-width: 44px; min-height: 44px; padding: 0; border-radius: 12px; border: 1px solid var(--border-strong); background: var(--panel); color: var(--muted2); font-size: 1.15rem; cursor: pointer; display: inline-flex; align-items: center; justify-content: center; transition: all 0.15s ease; }
  .check-btn:hover { border-color: var(--gold); color: var(--gold); }
  .check-btn.checked { background: #2d6a4f; border-color: #2d6a4f; color: #fffdf6; }
  .read-link { min-height: 44px; display: inline-flex; align-items: center; padding: 0.4rem 0.95rem; border-radius: 10px; border: 1px solid var(--border-strong); color: var(--teal-deep); text-decoration: none; font-size: 0.88rem; background: var(--panel2); transition: all 0.12s ease; }
  .read-link:hover { border-color: var(--teal); box-shadow: var(--shadow1); }
  .loading-msg { text-align: center; color: var(--muted2); padding: 2rem 0; }
  @media (max-width: 640px) {
    .chok-stats { grid-template-columns: repeat(3, 1fr); gap: 0.4rem; }
    .stat-num { font-size: 1.15rem; }
    .card-header { flex-direction: column; }
    .check-btn { align-self: flex-end; margin-top: -1.6rem; }
  }
</style>

  <script is:inline>
  (function() {
    'use strict';

    // === Hebrew Calendar Utilities ===

    // Tishrei 1 of year 5786 falls on approximately September 22, 2025
    const TISHREI_5786 = new Date(2025, 8, 22); // month is 0-indexed
    // Tishrei 1 of year 5787 falls on approximately September 11, 2026
    const TISHREI_5787 = new Date(2026, 8, 11);
    const DAYS_IN_YEAR = 354;

    const HEBREW_MONTHS = ['', 'תשרי', 'חשון', 'כסלו', 'טבת', 'שבט', 'אדר', 'ניסן', 'אייר', 'סיון', 'תמוז', 'אב', 'אלול'];
    const MONTH_DAYS = [0, 30, 29, 30, 29, 30, 29, 30, 29, 30, 29, 30, 29];
    const HEBREW_DAY_LETTERS = ['', 'א', 'ב', 'ג', 'ד', 'ה', 'ו', 'ז', 'ח', 'ט', 'י', 'יא', 'יב', 'יג', 'יד', 'טו', 'טז', 'יז', 'יח', 'יט', 'כ', 'כא', 'כב', 'כג', 'כד', 'כה', 'כו', 'כז', 'כח', 'כט', 'ל'];

    function getDayOfYear(date) {
      // Calculate which Jewish year we're in
      let tishrei;
      if (date >= TISHREI_5787) {
        // Year 5787: offset from TISHREI_5787
        tishrei = TISHREI_5787;
      } else if (date >= TISHREI_5786) {
        tishrei = TISHREI_5786;
      } else {
        // Before 5786, just use 5786 start and wrap
        tishrei = TISHREI_5786;
      }

      const diffMs = date.getTime() - tishrei.getTime();
      const diffDays = Math.floor(diffMs / (1000 * 60 * 60 * 24));

      // Wrap within 354 days (1-indexed)
      let dayOfYear = (diffDays % DAYS_IN_YEAR) + 1;
      if (dayOfYear <= 0) dayOfYear += DAYS_IN_YEAR;
      return dayOfYear;
    }

    function dayOfYearToHebrewDate(doy) {
      let remaining = doy;
      for (let m = 1; m <= 12; m++) {
        if (remaining <= MONTH_DAYS[m]) {
          return HEBREW_DAY_LETTERS[remaining] + ' ' + HEBREW_MONTHS[m];
        }
        remaining -= MONTH_DAYS[m];
      }
      return doy + '';
    }

    function dayOfYearToMonthDay(doy) {
      let remaining = doy;
      for (let m = 1; m <= 12; m++) {
        if (remaining <= MONTH_DAYS[m]) {
          return { month: m, day: remaining };
        }
        remaining -= MONTH_DAYS[m];
      }
      return { month: 12, day: 29 };
    }

    // === State ===
    let chokData = null;
    let currentDay = getDayOfYear(new Date());
    const STORAGE_KEY = 'chok-breslov';

    function getStorage() {
      try {
        return JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}');
      } catch { return {}; }
    }

    function setStorage(data) {
      localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
    }

    function isBookDoneForDay(bookId, day) {
      const store = getStorage();
      return !!(store.completed && store.completed[day] && store.completed[day][bookId]);
    }

    function toggleBookDone(bookId, day) {
      const store = getStorage();
      if (!store.completed) store.completed = {};
      if (!store.completed[day]) store.completed[day] = {};
      store.completed[day][bookId] = !store.completed[day][bookId];
      if (!store.completed[day][bookId]) delete store.completed[day][bookId];
      if (Object.keys(store.completed[day]).length === 0) delete store.completed[day];
      setStorage(store);
    }

    function getStreak() {
      const store = getStorage();
      if (!store.completed) return 0;
      const today = getDayOfYear(new Date());
      let streak = 0;
      for (let d = today; d >= 1; d--) {
        if (store.completed[d] && Object.keys(store.completed[d]).length > 0) {
          streak++;
        } else {
          break;
        }
      }
      return streak;
    }

    function getCompletedDays() {
      const store = getStorage();
      if (!store.completed) return 0;
      return Object.keys(store.completed).filter(d => Object.keys(store.completed[d]).length > 0).length;
    }

    // === Rendering ===

    function renderBookCard(book, day) {
      const portion = book.portions[day - 1];
      if (!portion || portion.sections.length === 0) return '';

      const isDone = isBookDoneForDay(book.id, day);
      const sections = portion.sections;
      const firstSection = sections[0];
      const lastSection = sections[sections.length - 1];

      let ref;
      if (sections.length === 1) {
        ref = book.sectionLabel + ' ' + firstSection.num;
      } else {
        ref = book.sectionLabel + ' ' + firstSection.num + '–' + lastSection.num;
      }

      // Calculate book-level completion
      const store = getStorage();
      let bookDaysCompleted = 0;
      if (store.completed) {
        for (const d in store.completed) {
          if (store.completed[d][book.id]) bookDaysCompleted++;
        }
      }
      const bookPercent = Math.round((bookDaysCompleted / DAYS_IN_YEAR) * 100);

      return '<div class="book-card' + (isDone ? ' completed' : '') + '" data-book="' + book.id + '" data-day="' + day + '">' +
        '<div class="card-header">' +
          '<div>' +
            '<p class="book-name-he">' + book.hebrewTitle + '</p>' +
            '<p class="book-name-en">' + book.title + '</p>' +
          '</div>' +
          '<button class="check-btn' + (isDone ? ' checked' : '') + '" data-book="' + book.id + '" data-day="' + day + '" aria-label="Mark ' + book.title + ' day ' + day + (isDone ? ' incomplete' : ' complete') + '" aria-pressed="' + (isDone ? 'true' : 'false') + '" title="' + (isDone ? 'Mark incomplete' : 'Mark complete') + '">' +
            (isDone ? '\u2713' : '') +
          '</button>' +
        '</div>' +
        '<p class="section-ref">' + ref + '</p>' +
        '<a href="' + firstSection.url + '" class="read-link">Read \u2192</a>' +
        '<div class="book-progress">' +
          bookPercent + '% of year completed' +
          '<div class="book-progress-bar"><div class="book-progress-fill" style="width:' + bookPercent + '%"></div></div>' +
        '</div>' +
      '</div>';
    }

    function renderDateLetters(day) {
      if (!chokData || !chokData.dateLetters) return;

      const md = dayOfYearToMonthDay(day);
      const matching = chokData.dateLetters.filter(dl => dl.month === md.month && dl.day === md.day);

      const section = document.getElementById('dateLettersSection');
      const content = document.getElementById('dateLettersContent');

      if (matching.length === 0) {
        section.style.display = 'none';
        return;
      }

      section.style.display = 'block';
      let html = '';
      for (const dl of matching) {
        for (const letter of dl.letters) {
          html += '<div class="date-letter-card">' +
            '<p class="letter-info">Ebay HaNachal Letter ' + letter.num + ' (Part ' + letter.part + ')</p>' +
            '<p class="letter-sub">Written on this day: ' + dl.hebrewDate + '</p>' +
            '<a href="' + letter.url + '">Read Letter \u2192</a>' +
          '</div>';
        }
      }
      content.innerHTML = html;
    }

    function renderBonus(day) {
      if (!chokData || !chokData.bonus) return;

      const bonus = chokData.bonus;
      const portion = bonus.portions[day - 1];
      if (!portion || portion.sections.length === 0) {
        document.getElementById('bonusSection').style.display = 'none';
        return;
      }

      document.getElementById('bonusSection').style.display = 'block';
      const sections = portion.sections;
      const first = sections[0];
      const last = sections[sections.length - 1];
      let ref = sections.length === 1
        ? bonus.sectionLabel + ' ' + first.num
        : bonus.sectionLabel + ' ' + first.num + '–' + last.num;

      document.getElementById('bonusContent').innerHTML =
        '<div class="book-card" data-book="' + bonus.id + '" data-day="' + day + '">' +
          '<div class="card-header">' +
            '<div>' +
              '<p class="book-name-he">' + bonus.hebrewTitle + '</p>' +
              '<p class="book-name-en">' + bonus.title + '</p>' +
            '</div>' +
            '<button class="check-btn' + (isBookDoneForDay(bonus.id, day) ? ' checked' : '') + '" data-book="' + bonus.id + '" data-day="' + day + '" aria-label="Mark ' + bonus.title + ' day ' + day + (isBookDoneForDay(bonus.id, day) ? ' incomplete' : ' complete') + '" aria-pressed="' + (isBookDoneForDay(bonus.id, day) ? 'true' : 'false') + '" title="' + (isBookDoneForDay(bonus.id, day) ? 'Mark incomplete' : 'Mark complete') + '">' +
              (isBookDoneForDay(bonus.id, day) ? '\u2713' : '') +
            '</button>' +
          '</div>' +
          '<p class="section-ref">' + ref + ' (Part ' + first.part + ')</p>' +
          '<a href="' + first.url + '" class="read-link">Read \u2192</a>' +
        '</div>';
    }

    function renderAll() {
      const day = currentDay;

      // Header
      document.getElementById('hebrewDateDisplay').textContent = dayOfYearToHebrewDate(day);
      document.getElementById('dayNumber').textContent = 'Day ' + day + ' of ' + DAYS_IN_YEAR;
      const percent = Math.round((day / DAYS_IN_YEAR) * 100);
      document.getElementById('progressFill').style.width = percent + '%';
      document.getElementById('progressText').textContent = 'Day ' + day + ' of ' + DAYS_IN_YEAR + ' \u2014 ' + percent + '% through the year';
      document.getElementById('dayPicker').value = day;

      // Stats
      document.getElementById('streakCount').textContent = getStreak();
      document.getElementById('completedCount').textContent = getCompletedDays();
      document.getElementById('overallPercent').textContent = Math.round((getCompletedDays() / DAYS_IN_YEAR) * 100) + '%';

      if (!chokData) return;

      // Portions grid
      let html = '';
      for (const book of chokData.books) {
        html += renderBookCard(book, day);
      }
      document.getElementById('portionsGrid').innerHTML = html;

      // Date letters
      renderDateLetters(day);

      // Bonus
      renderBonus(day);

    }

    // === Event handlers ===

    function setupEvents() {
      document.getElementById('prevDay').addEventListener('click', function() {
        currentDay = currentDay <= 1 ? DAYS_IN_YEAR : currentDay - 1;
        renderAll();
      });

      document.getElementById('nextDay').addEventListener('click', function() {
        currentDay = currentDay >= DAYS_IN_YEAR ? 1 : currentDay + 1;
        renderAll();
      });

      document.getElementById('todayBtn').addEventListener('click', function() {
        currentDay = getDayOfYear(new Date());
        renderAll();
      });

      document.getElementById('resetBtn').addEventListener('click', function() {
        currentDay = 1;
        renderAll();
      });

      document.getElementById('dayPicker').addEventListener('change', function() {
        let val = parseInt(this.value, 10);
        if (isNaN(val) || val < 1) val = 1;
        if (val > DAYS_IN_YEAR) val = DAYS_IN_YEAR;
        currentDay = val;
        renderAll();
      });

      // Delegate checkbox clicks
      document.addEventListener('click', function(e) {
        const btn = e.target.closest('.check-btn');
        if (!btn) return;
        const bookId = btn.dataset.book;
        const day = parseInt(btn.dataset.day, 10);
        toggleBookDone(bookId, day);
        renderAll();
      });
    }

    // === LT Calendar Override ===
    let ltCalendarMap = null;
    let ltUndatedIndex = 0; // tracks which undated prayer we're on

    function getLtPrayerForDay(day) {
      if (!ltCalendarMap) return null;
      const md = dayOfYearToMonthDay(day);
      // Check if there's a dated prayer for this Hebrew date
      const monthMap = ltCalendarMap.dateMap[md.month];
      if (monthMap && monthMap[md.day]) {
        const prayers = monthMap[md.day];
        return prayers.map(p => ({
          part: p.part,
          num: p.num,
          url: '/reader/likutay-tefilos/' + p.part + '/' + p.num,
          label: 'Prayer ' + p.num
        }));
      }
      // No dated prayer for today — use next undated prayer in sequence
      if (ltCalendarMap.undated && ltCalendarMap.undated.length > 0) {
        const idx = day % ltCalendarMap.undated.length;
        const p = ltCalendarMap.undated[idx];
        return [{
          part: p.part,
          num: p.num,
          url: '/reader/likutay-tefilos/' + p.part + '/' + p.num,
          label: 'Prayer ' + p.num
        }];
      }
      return null;
    }

    // === Init ===

    async function init() {
      setupEvents();
      renderAll(); // initial render with loading state

      try {
        const [chokResp, ltResp] = await Promise.all([
          fetch('/chok-breslov.json'),
          fetch('/lt-calendar-map.json')
        ]);
        if (!chokResp.ok) throw new Error('Could not load schedule');
        chokData = await chokResp.json();
        if (ltResp.ok) ltCalendarMap = await ltResp.json();
      } catch(e) {
        document.getElementById('portionsGrid').innerHTML =
          '<p class="loading-msg">Could not load daily study schedule. Please run: <code>node scripts/build-chok-breslov.cjs</code></p>';
        return;
      }

      // Override LT portions with calendar-based assignments
      if (chokData && ltCalendarMap) {
        const ltBook = chokData.books.find(b => b.id === 'likutay-tefilos');
        if (ltBook) {
          for (let day = 1; day <= 354; day++) {
            const ltPrayers = getLtPrayerForDay(day);
            if (ltPrayers && ltBook.portions[day - 1]) {
              ltBook.portions[day - 1].sections = ltPrayers;
            }
          }
        }
      }

      renderAll();
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init);
    } else {
      init();
    }
  })();
  </script>
</Layout>
