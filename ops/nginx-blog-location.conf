# Keep /blog assets on the dedicated blog tree.
# ^~ is required so generic CSS/JS regex locations do not override this alias.
location ^~ /blog {
    alias /opt/ajew-blog;
    index index.html;
    try_files $uri $uri/ /blog/index.html;
}
