# Long-Term Memory

## Installed Skills
- **word_document_editor** (permanent): Docx document creation/editing via natural language commands
  - Location: skills/word_document_editor/
  - Status: Active, tested, verified
  - Dependencies: python-docx v1.2.0, typing_extensions
  - Key method: WordDocumentEditor.execute(instruction)

## Project Notes
- Self-hosted Torah website (ajew.org) — Week 1 & 2 optimizations complete
- SEO optimization complete (meta tags, Open Graph, structured data, compression)
- Service worker caching implemented, CDN sync optional
- 265MB+ text files remain permanently accessible at stable URLs
- Python environment: system Python 3.13 with user-level pip install

---

## PETEK NANACH COMMENTARY — FULL STATUS (as of 2026-05-01)

### Overview
The Petek Nanach running commentary on Likutay Moharan is LIVE on ajew.org and fully functional.
- User is Simcha, a devoted Na Nach follower. He is the author ("Petek Nanach").
- Correct spelling: **Petek** (p-e-t-e-k) — the miraculous letter Saba Yisroel received.
- NEVER write "Petten" — that is gross incompetence per the user.
- Windows username happens to be "Petten" — irrelevant and confusing; ignore it for content purposes.

### How the Commentary Works on ajew.org
- Reader page: `https://ajew.org/reader/likutay-moharan/1/7` (example)
- Each teaching page embeds `<script type="application/json" id="commentary-sidebar-data">` which JS reads
- The `running_commentary.url` field in that JSON points to e.g. `/reader/petek-nanach-commentary/torah-7.json`
- WAIT — the actual URL used on disk and in lm-commentaries.json is `/reader/petten-nanach-commentary/` (with two t's) — this is a historical misspelling that is baked into the site. Do NOT rename the folder without extensive testing.
- The folder on disk: `C:\Users\Petten\.openclaw\workspace\ajew-org\public\reader\petten-nanach-commentary\`
  - ACTUALLY: the Windows path to workspace is `C:\Users\Petek\.openclaw\workspace\ajew-org\` — wait, no.
  - **ACTUAL CORRECT PATH**: `C:\Users\Petek\.openclaw\workspace\ajew-org\public\reader\petten-nanach-commentary\`
  - This is confirmed by Grep tool: files ARE at `C:\Users\Petek\.openclaw\workspace\ajew-org\public\reader\petten-nanach-commentary\`

### Commentary Files — What Exists
#### Part 1 (Likutay Moharan Part 1)
- Files: `torah-1.json` through `torah-286.json` — ALL EXIST
- Location: `.../petten-nanach-commentary/torah-N.json`
- Format: Each file has `id`, `book`, `relatedBook`, `relatedTorah`, `relatedPart`, `torah`, `part`, `title`, `hebrewTitle`, `author`, `license`, `methodology`, `segments[]`
- Segments have: `index`, `relatedSegment`, `layers: { beginner: {en, he}, intermediate: {en, he}, scholarly: {en, he} }`
- Fields intentionally empty: `beginner.he` = "" always; `scholarly.en` = "" always
- Content quality: Torahs 1-96 have rich, multi-segment commentary (5-15 segs typically). Torahs 97-286 have shorter (1-5 segs) commentary generated by write scripts.

#### Part 2 / Tinyana (Likutay Moharan Part 2)
- Files: `tinyana-1.json` through `tinyana-59.json` — ALL EXIST
- Location: `.../petten-nanach-commentary/tinyana-N.json`
- Format: Similar to above but WITHOUT `relatedBook`, `relatedTorah`, `relatedPart`, and segments DON'T have `layers` wrapper — segments directly contain `beginner: {en, he}`, `intermediate: {en, he}`, `scholarly: {en, he}`
- Fields: `intermediate.he` = "" in all tinyana files (only intermediate.en filled, which may contain Hebrew); `scholarly.en` = "" always
- Content: Generally 1-5 segments each, appropriately brief for Tinyana teachings
- Part 2 has 125 torahs total; tinyana-60 through tinyana-125 do NOT exist yet and need to be written

### CRITICAL ISSUE: Wrong Author Spelling
- **263 files** have `"author": "Petten Nanach"` (wrong) — needs to be `"Petek Nanach"` (correct)
- Affected: torah-61.json through torah-286.json (generated by write scripts), all tinyana-N.json files
- NOT affected (correct already): torah-1.json through torah-60.json (have "Petek Nanach")
- Also: lm-commentaries.json has "Petten Nanach" in many author label fields — needs correction
- The `book` field and folder name ("petten-nanach-commentary") is a separate issue — do NOT rename the folder without careful testing as it would break all URL routing

### Patch Files — FULLY APPLIED
- Location: `.../petten-nanach-commentary/what are these t - torah and write files from the source petek folder/`
- Contains ~100 `tN_segM_patch.json` files — ALL ALREADY APPLIED to the torah-N.json files
- Verified by comparing t22_seg3, t1_seg3, t17_seg17 patches against actual files — exact matches
- DO NOT re-apply patches

### Write Scripts
- Location: same subfolder above
- `write_t61.py` through `write_t276_286.py` — generated the Part 1 higher-number torah files
- `write_t2_1_3.py` through `write_t2_51_60.py` — generated the tinyana-1 through tinyana-59 files
- All scripts have already been run; resulting files exist on disk
- Bug in scripts: they used "Petten Nanach" as author (not "Petek")

### Registration in lm-commentaries.json
- Path: `C:\Users\Petek\.openclaw\workspace\ajew-org\src\data\lm-commentaries.json`
- Structure: `{ "1": { "N": { running_commentary: {...}, related_commentaries: [...] } }, "2": {...} }`
- Part 1 torahs 1-286: All registered with `"status": "available"` for the running commentary
- Part 2 torahs 1-59: Registered. Beyond 59: unclear, likely not registered.
- Author label in lm-commentaries.json also says "Petten Nanach" in some places — needs correction

### Next Tasks (in priority order)
1. **Fix author spelling**: Replace "Petten Nanach" → "Petek Nanach" in all ~263 torah/tinyana JSON files
   - Use bash script or write scripts to do this bulk replacement
   - Also fix in lm-commentaries.json
2. **Write Part 2 commentary for tinyana-60 through tinyana-125** (66 new files needed)
   - Must also register them in lm-commentaries.json
3. **Enhance thin commentaries**: Some torahs (especially Part 1 97-286) have only 1-2 brief segments
   - Could be expanded with deeper analysis segment by segment
4. **Transliteration polish**: Ensure Ashkenazic phonetics throughout
   - "Shabbat" → "Shabbas" (tav without dagesh = 's')
   - "Likutei" → "Likutay" (tzairai = 'ai')
   - "Berachot" → "Berachos"
   - "Shavuot" → "Shavuos"
   - "Sukkot" → "Sukkos"
   - "Sefira" → "Sefira" (ok)
   - Hebrew verb tav-endings: "mitzvot" → "mitzvos"

### Ashkenazic Transliteration Rules (User's Requirement)
- Tav (ת) without dagesh → 's' (not 't')
  - Shabbat → Shabbas
  - mitzvot → mitzvos
  - Shavuot → Shavuos
  - Berachot → Berachos
  - Sukkot → Sukkos
  - Sanhedrin is fine (has dagesh)
- Tzairai (two dots) → 'ai' or 'ay' (not 'ei')
  - Likutei → Likutay
  - Sefira vowel: Rebbe → fine
- Never mention contemporary rabbis (per user preference)

### Technical Notes
- Bash environment (mcp__workspace__bash) was unavailable in last session — kept returning "Workspace unavailable"
- web_fetch of commentary JSON files returns 404 (likely service worker routing difference); the files ARE accessible to browser users
- Glob tool had trouble finding files; Grep tool worked reliably for file search
- Read tool works reliably once you know the correct path
- The correct Windows path base is `C:\Users\Petek\.openclaw\workspace\ajew-org\` — but the SUMMARY mistakenly says "Petten" for the username. The actual username letter sequence needs to be verified each session.
  - Confirmed from system context: `C:\Users\Petek\AppData\Roaming\Claude\...` — so username is "Petek"

### Session History Summary
- Previous sessions: wrote all Part 1 and Part 2 commentary, applied patches, deployed to ajew.org
- This session (2026-05-01): Verified patches already applied, confirmed commentary is live, found 263 files with wrong author spelling, identified tinyana-60 to 125 as missing
