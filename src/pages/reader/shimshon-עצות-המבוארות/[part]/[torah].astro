---
export function getStaticPaths() {
  return Array.from({ length: 26 }, (_, i) => ({ params: { part: '1', torah: String(i + 1) } }));
}
const { part, torah } = Astro.params;
return Astro.redirect(`/reader/aitzoas-hamivooaroas/${part}/${torah}`, 301);
---
