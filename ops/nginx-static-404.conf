# ajew.org static-site fallback (inside the TLS server block)
# Unknown routes must return a real 404 rather than the homepage with HTTP 200.
error_page 404 /404.html;

location = /404.html {
    internal;
}

location / {
    try_files $uri $uri.html $uri/ =404;
}
